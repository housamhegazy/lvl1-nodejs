<nav class="navbar bg-body-tertiary">
  <div
    class="container-fluid justify-content-center justify-content-md-between"
  >
    <a class="navbar-brand fw-medium fs-3 mb-1 mb-md-0">
      Hegazy <sup>4</sup> dev</a
    >
    <% if (loggedInUsername ) { %>
    <span class="navbar-text me-3"> welcome : <%= loggedInUsername %> </span>
    <a href="/logout" class="btn btn-outline-light">Signout </a>
    <form action="/search" method="GET" class="d-flex" role="search">
      <input
        class="form-control me-2"
        type="text"
        name="searchname"
        placeholder="Search by name or email or phoneNumber..."
        required
      />
      <button class="btn btn-outline-success" type="submit">Search</button>
    </form>
    <% } else { %>
    <div>
      <a href="/login" class="btn btn-outline-light mx-2"> Signin</a>
    <a href="/register" class="btn btn-outline-light">Signup</a>
    </div>
    <% } %>
  </div>
</nav>
